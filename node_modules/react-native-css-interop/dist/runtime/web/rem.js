"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.rem = void 0;
const shared_1 = require("../../shared");
const signals_1 = require("../signals");
const isSSR = globalThis.window === undefined;
exports.rem = (function createRem(defaultValue = 16) {
    const signal = (0, signals_1.createSignal)(isSSR
        ? defaultValue
        : Number.parseFloat(globalThis.window.getComputedStyle(globalThis.window.document.documentElement).fontSize));
    const get = () => signal.get();
    const set = (nextValue) => {
        signal.set(nextValue || defaultValue);
        if (!isSSR) {
            globalThis.window.document.documentElement.style.fontSize = `${nextValue}px`;
        }
    };
    const reset = () => set(defaultValue);
    return { get, set, [shared_1.INTERNAL_RESET]: reset };
})();
//# sourceMappingURL=rem.js.map