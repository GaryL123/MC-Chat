import type { SelectorList } from "lightningcss";
import { Specificity, ExtractRuleOptions, CompilerStyleMeta, AttributeCondition } from "../types";
export type NormalizeSelector = {
    type: "rootVariables" | "universalVariables";
    subtype: "light" | "dark";
} | {
    type: "className";
    darkMode?: boolean;
    className: string;
    groupClassName?: string;
    pseudoClasses?: Record<string, true>;
    groupPseudoClasses?: Record<string, true>;
    attrs?: AttributeCondition[];
    specificity: Pick<Specificity, "A" | "B" | "C">;
};
export declare function normalizeSelectors(extractedStyle: CompilerStyleMeta, selectorList: SelectorList, options: ExtractRuleOptions, selectors?: NormalizeSelector[], defaults?: Partial<NormalizeSelector>): NormalizeSelector[];
